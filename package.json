{
    "name": "quotedrop-monorepo",
    "version": "1.0.0",
    "scripts": {
        "dev": "concurrently \"npm run client\" \"npm run server\"",
        "server": "cd server && npm start",
        "client": "cd client && npm run dev",
        "install:all": "npm install && cd client && npm install && cd ../server && npm install"
    },
    "devDependencies": {
        "concurrently": "^8.0.0"
    }
}